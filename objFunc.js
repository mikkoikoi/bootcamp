const user = {
    id : 24,
    displayName : "Kener",
    fullName : "Kendall Jenner",
}
function introduce ( {displayName, fullName}) {
    console.log(`${displayName} full name is ${fullName}`)
}

introduce(user)